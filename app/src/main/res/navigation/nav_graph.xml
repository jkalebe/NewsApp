<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/newsListFragment">

    <fragment
        android:id="@+id/newsListFragment"
        android:name="jota.kalebe.newsapp.NewListFragment"
        android:label="fragment_new_list"
        tools:layout="@layout/fragment_new_list" />

    <fragment
        android:id="@+id/newsListFav"
        android:name="jota.kalebe.newsapp.NewFavFragment"
        android:label="fragment_new_fav"
        tools:layout="@layout/fragment_new_fav" />

    <fragment
        android:id="@+id/settings"
        android:name="jota.kalebe.newsapp.SettingsFragment"
        android:label="fragment_settings"
        tools:layout="@layout/fragment_settings" />

    <fragment
        android:id="@+id/newsListSearch"
        android:name="jota.kalebe.newsapp.SearchFragment"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search" />

</navigation>